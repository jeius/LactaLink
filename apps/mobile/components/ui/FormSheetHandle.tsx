import React from 'react';
import { View } from 'react-native';

export default function FormSheetHandle() {
  return (
    <View className="h-10 items-center justify-center">
      <View className="h-1 w-12 rounded-full bg-primary-500" />
    </View>
  );
}

name: Publish preview update
defaults:
  tools:
    node: 22.14.0
    pnpm: 9.15.5

on:
  push:
    branches: ['preview']

jobs:
  publish_preview_update:
    name: Publish preview update
    type: update
    params:
      branch: ${{ github.ref_name || 'test' }}

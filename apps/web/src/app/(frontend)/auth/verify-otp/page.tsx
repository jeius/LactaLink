import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Verify OTP | LactaLink',
  description: 'Verify OTP page for LactaLink',
};

export default function Page() {
  return <div>Verify OTP page</div>;
}

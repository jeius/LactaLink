import { CollectionConfig } from 'payload';
import { Inventories } from './config';
import { InventoryAllocations } from './InventoryAllocations';

export const InventoryCollections: CollectionConfig[] = [Inventories, InventoryAllocations];
